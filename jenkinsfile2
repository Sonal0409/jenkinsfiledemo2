pipeline {
    agent any

    stages {
        stage('myjob1') {
            steps {
                sh 'touch file2'
            }
        }
        stage('myjob2') {
            steps {
                sh 'touch file3'
            }
        }
        stage('myjob3') {
            steps {
                sh 'touch file5'
            }
        }
    
      stage('myjob4') {
            steps {
                sh 'touch file6'
            }
        }
    }
}
